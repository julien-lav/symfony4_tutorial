 
{% extends 'base.html.twig' %}

{% block title %} Hello !{% endblock %}
{% block body %}

<div class="container">
   <h2>Add a good tutorial</h2>


   {{ form_start(form) }}

   {{ form_row(form.title) }}
   {{ form_row(form.content) }}
   {{ form_row(form.link) }}
   {{ form_row(form.createdAt) }}
   {#
   {{ form_row(form.user, {'attr': {'class': 'input-field col s12'}}) }}
   {{ form_row(form.category.name) }}
	 #}
   {{ form_end(form) }}

</div>

{% endblock %}